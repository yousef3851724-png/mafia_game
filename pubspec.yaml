name: mafia_radical
description: Mafia Radical - بازی مافیا بدون کارت فیزیکی
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.0.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
   - uses: actions/setup-java@v3
     with:
       distribution: 'temurin'
       java-version: '17'
