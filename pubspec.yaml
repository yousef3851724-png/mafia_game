name: mafia_radical
description: Mafia Radical - بازی مافیا بدون کارت فیزیکی
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

 dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.0   # این خط را اضافه کنید 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
