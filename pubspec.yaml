name: mafia_game
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # وابستگی‌های قبلی خودت را اینجا اضافه کن
  # مثال: 
  # cupertino_icons: ^1.0.8
  # provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # اگر Assets (عکس/فونت) داری، آنها را اینجا اضافه کن
